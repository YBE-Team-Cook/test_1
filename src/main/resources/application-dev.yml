spring:
  config:
    activate:
      on-profile: dev

  datasource:
    url: "jdbc:mysql://test-db:3306/test"
    username: 'root'
    password: 'root'
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true

    hibernate:
      ddl-auto: update

server:
  port: 8080
  error:
    include-message: always